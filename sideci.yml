linter:
  rubocop:
    root_dir: 'sideci_demo'

  phpmd:
    root_dir: 'sideci_demo'
    options:
      rule: 'phpmd.xml'

  code_sniffer:
    version: 3
    root_dir: 'sideci_demo'
    options:
        standard: PSR2
        encoding: utf-8